defmodule RockerAssignmentWeb.LoanApplicationControllerTest do
  use RockerAssignmentWeb.ConnCase

  describe "new/2" do
    test "Creates new loan"
    test "Returns error if attributes are invalid"
  end

  describe "all/2" do
    test "Returns list of all loans"
  end
end
